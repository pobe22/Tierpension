<Window x:Class="Tierpension.TiereAbholenScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tiere abholen" Height="400" Width="400">
    <Grid>
        <ListBox x:Name="BuchungenListBox" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" DisplayMemberPath="."/>
        <Button Content="Jetzt abholen" Click="JetztAbholen_Click" Margin="10" Height="25" Width="100"/>
    </Grid>
</Window>
